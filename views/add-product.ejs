<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %></title>
    <link rel="stylesheet" href="/css/products.css">
</head>
<body>
    <h1><%= title %></h1>

    
    <form action="<% if (editing) { %> /admin/edit-product <% } else { %>/admin/product<% } %>" method="POST" class="add-product-form">
        <% if (editing) { %>
            <input type="hidden" name="productId" value="<%= product._id %>" >
            <input type="hidden" name="productPrice" value="<%= product.price %>" >
        <% } %>
        <input type="text" name="message" value="<% if (editing) { %><%= product.title %><% } %>"/>
        <button type="submit"><% if (editing) { %>Update Product<%} else { %>Add product<% } %></button>
    </form>
</body>
</html>